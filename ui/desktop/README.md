# UI Desktop

Desktop packaging and release artifacts are produced from the Flutter project in `ui/mobile` using `lib/main_desktop.dart`.

## Build command
- `powershell -ExecutionPolicy Bypass -File infra/scripts/build_desktop.ps1 -Configuration release`

## Output
- `builds/desktop/Release`
